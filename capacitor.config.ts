import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.perf.react',
  appName: 'ionic-perf-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
